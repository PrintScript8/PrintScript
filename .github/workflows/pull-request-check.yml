name: Verify CI

on:
    pull_request:
        branches:
          - 'master'
          - 'dev'

jobs:
  workflow_call:
    uses: PrintScript8/ingsis-library/blob/main/.github/workflows/on-pull-request-printscript.yml@main
