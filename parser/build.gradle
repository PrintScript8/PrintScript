plugins {
    id 'application-conventions'
    id 'io.gitlab.arturbosch.detekt' // Ensure Detekt is applied
}

dependencies {
    implementation project(':lexer')
}

kotlin {
    jvmToolchain(18)
}
kover {
    filters {
        classes {
            excludes.add("node.**")
        }
    }
}